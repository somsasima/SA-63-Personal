'use strict';

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

require('commander');
require('chalk');
require('./index-4e91ee76.js');
var fs = _interopDefault(require('fs-extra'));
require('@backstage/cli-common');
require('path');
require('child_process');
require('util');
require('./run-f8afca8f.js');
require('tar');
require('os');
var index$1 = require('./index-92997c05.js');

var buildWorkspace = async (dir, _cmd, packages) => {
  if (!await fs.pathExists(dir)) {
    throw new Error(`Target workspace directory doesn't exist, '${dir}'`);
  }
  await index$1.createDistWorkspace(packages, {
    targetDir: dir
  });
};

exports.default = buildWorkspace;
